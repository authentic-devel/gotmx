version: "2"

linters:
  # Explicitly listed so a golangci-lint upgrade never silently
  # changes which linters are active.
  # Note: gosimple is now merged into staticcheck in v2.
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
